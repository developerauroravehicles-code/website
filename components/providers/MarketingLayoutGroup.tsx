"use client";

import { LayoutGroup } from "framer-motion";

export function MarketingLayoutGroup({ children }: { children: React.ReactNode }) {
  return <LayoutGroup id="aurora-marketing">{children}</LayoutGroup>;
}
